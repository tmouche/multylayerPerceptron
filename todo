
- parsing:
    - dataset
    - args

- archi
    - network class

- ya un probleme dans le sujet: demande d'utiliser softmax (incombe d'avoir au moins deux output)\
    mais demande aussi l'utilisation de Binary Cross Entropy (pour 1 neuron)
    pour pallier a ca il va falloir faire les deux. Attention il va falloir check les inputs,

    - si softmax 1 output -> throw x
    - si softmax avec Binary cross entropy -> throw x
    - si Binary Cross entropy n output > 1 -> throw x

- ya des plots a faire sur le training

- peut etre qu il faut seed les randoms pour que quand on benchmark deux trucs on puisse avoir la meme initialisation

- ATTENTION AUX MATH DOMAINS ERRORS

- la back prop fonctionne pas lol

- revoir la maniere dont les weigths sont updated